<div class="main">
    <form action="/login" method="POST" class="form" id="form-login">
        <h3 class="heading">Đăng nhập</h3>
        <p class="desc">Đăng ký tài khoản để làm thành viên của chúng tôi ❤️</p>
        {{#if error}}
            <p style="margin-top:8px;font-size:14px;color:red">{{error}}</p>
        {{/if}}
        <div class="form-group">
        <label for="username" class="form-label">Email</label>
        <input id="username" name="username" type="text" placeholder="VD: email@domain.com" class="form-control">
        <span class="form-message"></span>
        </div>

        <div class="form-group">
        <label for="password" class="form-label">Mật khẩu</label>
        <input id="password" name="password" type="password" placeholder="Nhập mật khẩu" class="form-control">
        <span class="form-message"></span>
        </div>

        <button class="form-submit">Đăng nhập</button>
        <p class="desc">Bạn chưa có tài khoản ? <a href="/register" class="link">Đăng Ký</a></p>
    </form>
</div>
 {{!-- Xu ly thong bao tu register --}}
<script>
    const successMessage = "{{successMessage}}";
    if (successMessage) {
        alert(successMessage);
    }
</script>


   